{
  "name": "team-workload-netlify-apps-script",
  "private": true,
  "version": "1.2.0",
  "description": "Netlify frontend + Apps Script Fast API v1.2 + existing Google Sheet",
  "type": "module",
  "scripts": {
    "dev": "npx netlify-cli dev",
    "deploy": "npx netlify-cli deploy --prod --dir public --functions netlify/functions"
  },
  "engines": {
    "node": ">=18"
  }
}